/* eslint-disable @typescript-eslint/no-duplicate-enum-values */
const enum GRID {
  countRows = 15,
  countColumns = 15,
  cellWidthAndHeigth = 30,
}

const enum CANVAS_SIZE {
  width = 800,
  height = 600,
}

export { CANVAS_SIZE, GRID };
